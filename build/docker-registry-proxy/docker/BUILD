subinclude("@pleasings//docker")

docker_image(
    name = "docker",
    dockerfile = "docker-registry-proxy.Dockerfile",
    image = "docker-registry-proxy",
    srcs = ["//cmd/docker-registry-proxy"],
)
